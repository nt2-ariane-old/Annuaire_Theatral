<div{{ attributes }} >
    {% for item in items %}
        <div class="blog-minimal">
            <div class="data-blog"><i class="fas fa-address-card"></i></div>

            <p>{{ item.content }}</p>
        </div>

    {% endfor %}
</div>