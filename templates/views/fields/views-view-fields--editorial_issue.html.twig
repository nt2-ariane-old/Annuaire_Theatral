<div class="editorial_infos">
    <h3>{{ fields.title.content }}</h3>
    <h4>{{ fields.field_author.content }}</h4>
    <p>{{ fields.body.content }}</p>
    {% for key in fields.field_author|keys %}
        {{ key }}
    {% endfor %}
</div>