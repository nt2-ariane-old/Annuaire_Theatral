<div{{attributes}} class="product">

	<section class="w-section product-post-banner " style="background-image:url('{{ file_url(product_entity.field_image_de_couverture.entity.fileuri) }}')">
		<div class="align-center"><h2 class="inner-title">{{product.title}}</h2></div>
	</section>

	<section id="sommaire">
		<p>
			{{product.body}}
			{{product.variations}}
		</p>
	</section>
</div>
